define(["scripts", "localstorage"], function (ReltioManager) {
    ReltioManager.module("Entities", function (Entities, ReltioManager, Backbone, Marionette, $, _) {
// edited for brevity
    });

    return ReltioManager.Entities.configureStorage;
});